---
import { ui, type Lang } from '../i18n/ui';
interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = ui[lang];
---

<footer class="site-footer">
  <span>{t.footer.note}</span>
</footer>

<style>
  .site-footer .middle {
    color: var(--accent);
    letter-spacing: 0.1em;
  }
  @media (max-width: 720px) {
    .site-footer .middle { display: none; }
  }
</style>
